export default class StringCombo {
  constructor(input, output) {
    this.input = input;
    this.output = output;
  }

  getInput() {
    return this.input;
  }

  getOutput() {
    return this.output;
  }
}
